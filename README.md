# testgit
Simple steps for Git;

1.  Clone repo
2.  Set directory in the cloned repo (1) 
->  $cd [path of the cloned repo]
3.  Update/change README.md 
4.  $ git add README.md
5.  $ git commit -m "message"
6.  $ git push 
